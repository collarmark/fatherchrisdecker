---
layout: base.njk
title: About
permalink: /about/
---

<section class="page-hero">
  <div class="container">
    <h1>About Father Chris Decker</h1>
  </div>
</section>

<section class="section">
  <div class="container prose">
    {% for page in collections.page %}
      {% if page.data.slug == 'about' or page.fileSlug == 'about' %}
        {{ page.content | safe }}
      {% endif %}
    {% endfor %}

    {# Fallback if no CMS page exists yet #}
    {% if not collections.page or collections.page.length == 0 %}
    <p>Father Chris Decker is a Catholic priest, artist, and pilgrim. This site gathers his reflections on faith, travel, and the creative life.</p>
    {% endif %}
  </div>
</section>
