{
  "name": "fatherchrisdecker",
  "version": "1.0.0",
  "description": "fatherchrisdecker.com – Eleventy + TinaCMS",
  "scripts": {
    "dev": "tinacms dev --port 9001 -c \"npx @11ty/eleventy --serve --port 8082 --quiet\"",
    "build": "tinacms build && npx @11ty/eleventy",
    "tina:build": "tinacms build && npx @11ty/eleventy",
    "eleventy": "npx @11ty/eleventy",
    "eleventy:serve": "npx @11ty/eleventy --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "npm": "^11.12.0",
    "start": "^5.1.0",
    "tinacms": "^2.6.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.6.0"
  }
}
